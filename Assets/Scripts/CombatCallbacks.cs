using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CombatCallbacks : MonoBehaviour
{

    // Delegates
    public delegate void delBeforeHit();
    public delegate void delAfterHit();
    public delegate void delBeforeKnockback();
    public delegate void delAfterKnockback();


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
